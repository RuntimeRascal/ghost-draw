<UserControl x:Class="GhostDraw.Views.UserControls.ScreenshotSavedToastControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="280">
    <Border x:Name="Root"
            Padding="12,8"
            Background="#2A2A2A"
            CornerRadius="4"
            Opacity="0"
            IsHitTestVisible="False">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.6" BlurRadius="10" ShadowDepth="0"/>
        </Border.Effect>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="&#xE73E;"
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="18"
                       Foreground="#8BC34A"/>
            <StackPanel Margin="8,0,0,0">
                <TextBlock x:Name="TitleText" Text="Screenshot saved" FontSize="14" Foreground="#F0F0F0"/>
                <TextBlock x:Name="PathText" Text="" FontSize="12" Foreground="#B0B0B0" TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
