<UserControl x:Class="GhostDraw.Views.UserControls.CanvasClearedToastControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="160" d:DesignWidth="320">
    <Border x:Name="Root"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Padding="24,16"
            Background="#1A1A2E"
            BorderBrush="#00FF88"
            BorderThickness="2"
            CornerRadius="4"
            Opacity="0"
            IsHitTestVisible="False">
        <Border.Effect>
            <DropShadowEffect Color="#00FF88" 
                              Opacity="0.7" 
                              BlurRadius="20" 
                              ShadowDepth="0"/>
        </Border.Effect>
        <TextBlock x:Name="ToastText"
                   FontFamily="Segoe UI"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#00FF88"
                   Text="Canvas Cleared"/>
    </Border>
</UserControl>
