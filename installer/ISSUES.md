# Installer Issues

- No desktop shortcut created.
- No startup item created.
- what is `build_old.ps1`. Should be removed?

# App Issues
- if app is running and launch app again a new app instance is created rather then using current app instance.